namespace CodingTask2
{
   public class AStarPathFinder : IPathFinder
   {
      public PathInfo FindPath(Coord startLocation, Coord finalLocation)
      {
         return PathInfo.NotExistingPath();
      }
   }
}